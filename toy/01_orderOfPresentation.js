function orderOfPresentation(N, K) {
    // 문제가 뭔말인지 모르겠음ㅋㅋㅋㅋㅋㅋㅋㅋㅋ...........
    // 테스트 코드에서 아예 오류 먹어서 테스트가 안됨.....
    // 개발자 모드에서는 되는데 도대체 뭐가 문제냐

    // let numbers = [];
    // for(let i=1; i<=N; i++) {
    //   numbers.push(i);
    // }
    // let result = [];
    // const perm = (arr, m = []) => {
    //   if(arr.length === 0) {
    //     result.push(m);
    //   } else {
    //     for(let i=0; i<arr.length; i++) {
    //       let currentArr = arr.slice();
    //       let element = currentArr.splice(i,1);
    //       perm(currentArr.slice(), m.concat(element));
    //     }
    //   }
    // };
    // perm(numbers);

    // if(Array.isArray(K)) {
    //   return result.findIndex((el) => {
    //     return String(el) === String(K);
    //   })

    // } else {
    //   return result[K];
    // }
}